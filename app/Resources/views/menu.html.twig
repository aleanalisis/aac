
    {% block menu %}
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ path('aac/inicio') }}">
                        <img src="{{asset('bundles/aac/images/Analisis05.png')}}" alt="Análisis y Auditoria de Cuentas"/>
                        &nbsp;Análisis y Auditoria de Cuentas
                    </a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {% if is_granted('ROLE_REGISTRADO') %}
                            
                            <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                Usuarios 
                                <span class="caret"></span>
                            </a>
                                <ul class="dropdown-menu" role="menu">
                                {% if is_granted('ROLE_INTERVENTOR') %}                                    
                                    <li class="">
                                        <a href="{{ path('usuarios_entrada') }}">
                                            Usuarios en Activo
                                        </a>
                                    </li>
                                    <li class="divider"></li>
                                {% endif %}
                                    
                                    <li>
                                        <a href="{{ path('archivos') }}">
                                            Descargar Archivos
                                        </a>
                                    </li>
                                    <li>
                                        <a href="{{ path('archivos_subir') }}">
                                            Subir Archivos
                                        </a>
                                    </li>                                
                                </ul> <!-- FIN class="dropdown-menu" -->
                            </li>
                            {% endif %}
                    {% endif %}
                    </ul><!-- FIN nav navbar-nav -->
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <!-- BOTON CERRAR SESIÓN -->
                        <ul class="nav navbar-nav navbar-right">
                            <li>
                                <form class="navbar-form navbar-right" role="form" method="post" 
                                      action="{{ path('fos_user_security_logout') }}">
                                    <button type="submit" class="btn btn-primary btn-sm">
                                        <span class="glyphicon glyphicon-log-out"></span>
                                    &nbsp;Cerrar Sesión                                
                                    </button>
                                </form>
                            </li>
                        </ul>
                    {% else %}
                        <!-- BOTON REGISTRARSE -->
                        <ul class="nav navbar-nav navbar-right">
                            <li class="{#<?php echo $this->registro ?>#}">
                                <form class="navbar-form navbar-right" role="form" method="post" 
                                      action="{{ path('fos_user_registration_register') }}">
                                    <button type="submit" class="btn btn-primary btn-sm">
                                        <span class="glyphicon glyphicon-user"></span>
                                    &nbsp;Registrarse                                
                                    </button>
                                </form>
                            </li>
                        </ul>
                        <!-- BOTON LOGIN -->
                        <form class="navbar-form navbar-right" role="form" method="post" 
                              action="{{ path('fos_user_security_login') }}">
                            <button type="submit" class="btn btn-success btn-sm">
                               <span class="glyphicon glyphicon-log-in"></span> 
                               &nbsp; Login
                            </button>
                        </form>
                    {% endif %}
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {% if is_granted('ROLE_INTERVENTOR') %}
                        <ul class="nav navbar-nav">                        
                        <li class="">
                            <a href="{{ path('contacto_lista') }}">
                                Contactos 
                            </a>
                        </li>
                        </ul>
                        {% endif %} <!--  Fin ROLE_INTERVENTOR -->
                    {% else %}
                        <ul class="nav navbar-nav navbar-right">                        
                            <li class="">
                                <a href="{{ path('contacto') }}">
                                    Contacto 
                                </a>
                            </li>
                        </ul>                        
                    {% endif %}
                    <ul class="nav navbar-nav navbar-right">                        
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                Servicios
                                <span class="caret"></span>
                            </a>
                                <ul class="dropdown-menu" role="menu">
                                    <li class="">
                                        <a href="{{ path('aac/servicios/profesionales') }}">
                                            Auditorias
                                        </a>
                                    </li>
                                    <li>
                                        <a href="{{ path('aac/servicios/voluntarios') }}">
                                            Auditorias Voluntarias
                                        </a>
                                    </li>
                                    <li>
                                        <a href="{{ path('aac/otros/servicios') }}">
                                            Otros Servicios 
                                        </a>
                                    </li>                                
                                </ul> <!-- FIN class="dropdown-menu" -->
                        </li>
                    </ul>

                </div><!--/.nav-collapse -->
            </div>
        </nav>
    {% endblock %}
    

    

